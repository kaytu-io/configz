Connector: AWS
Description: Ensure Auto Minor Version Upgrade is enabled for RDS to automatically receive minor engine upgrades during the maintenance window.
Enabled: true
ID: aws_rds_auto_minor_version_upgrade
Internal: false
Links: null
LogoURL: ""
LongTitle: Ensure Auto Minor Version Upgrade is enabled for RDS to automatically receive minor engine upgrades during the maintenance window.
RiskLevel: Medium
Query:
  Connector: AWS
  Engine: odysseus-v0.0.1
  ID:
  ListOfTables:
    - aws_rds_db_instance
  PrimaryTable: aws_rds_db_instance
  QueryToExecute: |- 
    select
      db_instance_identifier as resource,
      kaytu_account_id as kaytu_account_id,
      kaytu_resource_id as kaytu_resource_id,
      case
        when not auto_minor_version_upgrade then 'alarm'
        else 'ok'
      end as status,
      case
        when not auto_minor_version_upgrade then title || ' automatic minor version upgrades not enabled.'
        else title || ' automatic minor version upgrades enabled.'
      end as reason,
      region,
      account_id
    from
      aws_rds_db_instance
ShortTitle: RDS Auto Minor Version Upgrade
Tags:
  category: