{
  "ID": "aws_tag_dimensions_sql.common_dimensions_sql_large_ebs_volumes",
  "Engine": "odysseus-v0.0.1",
  "QueryToExecute": "select\n  arn as resource,\n  case\n    when size \u003c= $1 then 'ok'\n    else 'alarm'\n  end as status,\n  volume_id || ' is ' || size || 'GB.' as reason\n  \n  , region, account_id\nfrom\n  aws_ebs_volume;\n",
  "Connector": "AWS",
  "PrimaryTable": "aws_ebs_volume",
  "ListOfTables": [
    "aws_ebs_volume"
  ]
}