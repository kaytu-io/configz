{
  "ID": "aws_untagged_sql_iam_server_certificate_untagged",
  "Engine": "odysseus-v0.0.1",
  "QueryToExecute": "select\n  arn as resource,\n  case\n    when tags = '{}' or tags is null then 'alarm'\n    else 'ok'\n  end as status,\n  case\n    when tags = '{}' or tags is null then title || ' has no tags.'\n    else title || ' has tags.'\n  end as reason\n  \n  , region, account_id\nfrom\n  aws_iam_server_certificate;\n",
  "Connector": "AWS",
  "PrimaryTable": "aws_iam_server_certificate",
  "ListOfTables": [
    "aws_iam_server_certificate"
  ]
}