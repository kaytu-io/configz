{
  "ID": "aws_tag_dimensions_sql.common_dimensions_sql_cloudfront_distribution_pricing_class",
  "Engine": "odysseus-v0.0.1",
  "QueryToExecute": "select\n  id as resource,\n  case\n    when price_class = 'PriceClass_All' then 'info'\n    else 'ok'\n  end as status,\n  title || ' has ' || price_class || '.'\n  as reason\n  \n  , region, account_id\nfrom\n  aws_cloudfront_distribution;\n",
  "Connector": "AWS",
  "PrimaryTable": "aws_cloudfront_distribution",
  "ListOfTables": [
    "aws_cloudfront_distribution"
  ]
}