{
  "ID": "azure_mandatory_sql_resource_group_compute_image_mandatory",
  "Engine": "odysseus-v0.0.1",
  "QueryToExecute": "with analysis as (\n  select\n    id,\n    title,\n    tags ?\u0026 $1 as has_mandatory_tags,\n    to_jsonb($1) - array(select jsonb_object_keys(tags)) as missing_tags,\n    resource_group, subscription_id\n  from\n    azure_compute_image\n)\nselect\n  id as resource,\n  case\n    when has_mandatory_tags then 'ok'\n    else 'alarm'\n  end as status,\n  case\n    when has_mandatory_tags then title || ' has all mandatory tags.'\n    else title || ' is missing tags: ' || array_to_string(array(select jsonb_array_elements_text(missing_tags)), ', ') || '.'\n  end as reason,\n  resource_group, subscription_id\nfrom\n  analysis\n",
  "Connector": "Azure",
  "PrimaryTable": "azure_compute_image",
  "ListOfTables": [
    "azure_compute_image"
  ]
}