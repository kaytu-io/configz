{
  "ID": "azure_tag_dimensions_sql.common_dimensions_qualifier_sql.common_dimensions_subscription_sql_sql_database_long_running_reserved_capacity",
  "Engine": "odysseus-v0.0.1",
  "QueryToExecute": "select\n  db.id as resource,\n  case\n    when date_part('day', now() - creation_date) \u003e $1 then 'alarm'\n    when date_part('day', now() - creation_date) \u003e $2 then 'info'\n    else 'ok'\n  end as status,\n  db.title || ' has been in use for ' || date_part('day', now() - creation_date) || ' day(s).'\n  as reason\n  \n  , db.resource_group\n  , display_name as subscription\nfrom\n  azure_sql_database as db,\n  azure_subscription as sub\nwhere\n  db.name != 'master'\n  and db.subscription_id = sub.subscription_id;\n",
  "Connector": "Azure",
  "PrimaryTable": "azure_sql_database",
  "ListOfTables": [
    "azure_sql_database",
    "azure_subscription"
  ]
}