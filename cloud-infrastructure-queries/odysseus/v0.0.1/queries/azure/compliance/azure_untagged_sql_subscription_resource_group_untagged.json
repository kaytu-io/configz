{
  "ID": "azure_untagged_sql_subscription_resource_group_untagged",
  "Engine": "odysseus-v0.0.1",
  "QueryToExecute": "    select\n      id as resource,\n      case\n        when tags is not null then 'ok'\n        else 'alarm'\n      end as status,\n      case\n        when tags is not null then title || ' has tags.'\n        else title || ' has no tags.'\n      end as reason,\n      subscription_id\n    from\n      azure_resource_group\n",
  "Connector": "Azure",
  "PrimaryTable": "azure_resource_group",
  "ListOfTables": [
    "azure_resource_group"
  ]
}