{
  "ID": "azure_tag_dimensions_sql.common_dimensions_qualifier_sql.common_dimensions_subscription_sql_network_public_ip_unattached",
  "Engine": "odysseus-v0.0.1",
  "QueryToExecute": "select\n  ip.id as resource,\n  case\n    when ip.ip_configuration_id is null then 'alarm'\n    else 'ok'\n  end as status,\n  case\n    when ip.ip_configuration_id is null then ip.title  || ' has no associations.'\n    else ip.title || ' associated with network interface ' || split_part(ip.ip_configuration_id, '/', 9) || '.'\n  end as reason\n  \n  , ip.resource_group\n  , display_name as subscription\nfrom\n  azure_public_ip as ip,\n  azure_subscription as sub\nwhere\n  sub.subscription_id = ip.subscription_id;\n",
  "Connector": "Azure",
  "PrimaryTable": "azure_public_ip",
  "ListOfTables": [
    "azure_public_ip",
    "azure_subscription"
  ]
}