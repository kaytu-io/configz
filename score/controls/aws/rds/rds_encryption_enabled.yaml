Connector: AWS
Description: Ensure encryption is setup for RDS instances to fulfill compliance requirements for data-at-rest encryption.
Enabled: true
ID: 5.0
Internal: false
Links: null
LogoURL: ""
LongTitle: Ensure encryption is setup for RDS instances to fulfill compliance requirements for data-at-rest encryption.
RiskLevel: High
Query:
  Connector: AWS
  Engine: odysseus-v0.0.1
  ID:
  ListOfTables:
    - aws_rds_db_instance
  PrimaryTable: aws_rds_db_instance
  QueryToExecute: |-
    select
      db_instance_identifier as resource,
      kaytu_account_id as kaytu_account_id,
      kaytu_resource_id as kaytu_resource_id,
      case
        when storage_encrypted then 'ok'
        else 'alarm'
      end as status,
      case
        when storage_encrypted then title || ' encrypted at rest.'
        else title || ' not encrypted at rest.'
      end as reason,
      region,
      account_id
    from
      aws_rds_db_instance;
ShortTitle: RDS Encryption Enabled
Tags:
  category: