Connector: AWS
Description: Enable RDS Copy Tags to Snapshots.
Enabled: true
ID: 5.0
Internal: false
Links: null
LogoURL: ""
LongTitle: Enable RDS Copy Tags to Snapshots.
RiskLevel: Low
Query:
  Connector: AWS
  Engine: odysseus-v0.0.1
  ID:
  ListOfTables:
    - aws_rds_db_instance
  PrimaryTable: aws_rds_db_instance
  QueryToExecute: |- 
    select
      db_instance_identifier as resource,
      kaytu_account_id as kaytu_account_id,
      kaytu_resource_id as kaytu_resource_id,
      case
        when not copy_tags_to_snapshot then 'alarm'
        else 'ok'
      end as status,
      case
        when not copy_tags_to_snapshot then title || ' copy Tags to Snapshots feature is not enabled.'
        else title || ' copy Tags to Snapshots feature is enabled.'
      end as reason,
      region,
      account_id
    from
      aws_rds_db_instance
    where
      engine = 'mysql' or engine = 'postgres'
ShortTitle: RDS Copy Tags to Snapshots
Tags:
  category: