{
  "connectors": [
    "AWS"
  ],
  "status": "active",
  "name": "DynamoDB Accelerator (DAX)",
  "query": "SELECT kaytu_account_id, period_start::date::text as date, sum(amortized_cost_amount) FROM aws_cost_by_service_daily WHERE service = 'DynamoDB Accelerator (DAX)' group by 1,2;",
  "tables": [
    "DynamoDB Accelerator (DAX)"
  ],
  "tags": {
    "category": [
      "Database"
    ]
  }
}