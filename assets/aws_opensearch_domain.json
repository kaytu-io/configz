
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "OpenSearch (ElasticSearch) Domains",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::opensearch::domain' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
