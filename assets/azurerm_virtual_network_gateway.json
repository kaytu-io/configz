
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Virtual network gateway",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/virtualnetworkgateways' group by 1,2;",
    "tags": {
        "category": ["Network Gateways"]
    }
}
