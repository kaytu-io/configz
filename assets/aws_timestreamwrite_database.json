
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "Timestream Database",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::timestream::database' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
