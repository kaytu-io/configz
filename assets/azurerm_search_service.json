
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Azure Cognitive Search",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.search/searchservices' group by 1,2;",
    "tags": {
        "category": ["AI + ML"]
    }
}
