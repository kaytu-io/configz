
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "NetApp Account",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.netapp/netappaccounts' group by 1,2;",
    "tags": {
        "category": ["Storage"]
    }
}
