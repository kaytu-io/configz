connectors:
- AWS
name: Network Firewall
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::networkfirewall::firewall' group by 1,2;
status: active
tags:
  category:
  - Network
