
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Virtual machine scale set",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.compute/virtualmachinescalesets' group by 1,2;",
    "tags": {
        "category": ["Compute"]
    }
}
