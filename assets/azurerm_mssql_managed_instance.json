
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Microsoft SQL Azure Managed Instance",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.sql/managedinstances' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
