
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "CDN profile",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.cdn/profiles' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
