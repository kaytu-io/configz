
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "Network Load Balancer",
    "query": "select kaytu_account_id, region, count(*) from aws_ec2_network_load_balancer group by 1,2;",
    "tables": ["aws::elasticloadbalancing::loadbalancer"],
    "tags": {
        "category": ["Network"]
    }
}
