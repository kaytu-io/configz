
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "OpsWorks Config Management Instance",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::opsworkscm::server' group by 1,2;",
    "tags": {
        "category": ["Management and governance"]
    }
}
