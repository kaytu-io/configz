
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Availability set",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.compute/availabilitysets' group by 1,2;",
    "tags": {
        "category": ["Compute"]
    }
}
