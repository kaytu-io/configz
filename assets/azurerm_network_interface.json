
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Network interface (NIC)",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/networkinterfaces' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
