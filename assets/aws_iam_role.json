
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "IAM Role",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::iam::role' group by 1,2;",
    "tags": {
        "category": ["Identity + Access"]
    }
}
