
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "NAT gateway",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/natgateways' group by 1,2;",
    "tags": {
        "category": ["Network Gateways"]
    }
}
