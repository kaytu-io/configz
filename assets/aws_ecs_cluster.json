
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "Elastic Container Service (ECS) Cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ecs::cluster' group by 1,2;",
    "tags": {
        "category": ["Containers"]
    }
}
