
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Web app",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.web/sites' group by 1,2;",
    "tags": {
        "category": ["PaaS"]
    }
}
