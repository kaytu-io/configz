
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "HDInsight cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.hdinsight/clusters' group by 1,2;",
    "tags": {
        "category": ["Data Analytics"]
    }
}
