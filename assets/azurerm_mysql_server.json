
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "MySQL Server",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.dbformysql/servers' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
