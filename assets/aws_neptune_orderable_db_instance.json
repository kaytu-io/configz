
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "Neptune Database",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::neptune::database' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
