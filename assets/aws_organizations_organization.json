
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "Organization",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::organizations::organization' group by 1,2;",
    "tags": {
        "category": ["Management and governance"]
    }
}
