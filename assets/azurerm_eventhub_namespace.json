
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Event Hubs namespace",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.eventhub/namespaces' group by 1,2;",
    "tags": {
        "category": ["Data Analytics"]
    }
}
