
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Batch accounts",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.batch/batchaccounts' group by 1,2;",
    "tags": {
        "category": ["Compute"]
    }
}
