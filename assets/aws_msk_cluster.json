
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "Managed Kafka Cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::kafka::cluster' group by 1,2;",
    "tags": {
        "category": ["Managed Services"]
    }
}
