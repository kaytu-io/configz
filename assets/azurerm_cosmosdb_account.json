
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Azure Cosmos DB Account",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.documentdb/databaseaccounts' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
