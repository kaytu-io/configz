
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Azure Cognitive Services",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.cognitiveservices/accounts' group by 1,2;",
    "tags": {
        "category": ["AI + ML"]
    }
}
