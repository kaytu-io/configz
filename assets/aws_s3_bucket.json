
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "S3 Bucket",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::s3::bucket' group by 1,2;",
    "tags": {
        "category": ["Storage"]
    }
}
