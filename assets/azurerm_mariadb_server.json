
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "MariaDB server",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.dbformariadb/servers' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
