
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "Managed Prometheus Workspace",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::amp::workspace' group by 1,2;",
    "tags": {
        "category": ["Managed Services"]
    }
}
