connectors:
- AWS
name: Redshift (Serverless) Namespace
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::redshiftserverless::namespace' group by 1,2;
status: active
tags:
  category:
  - Database
