connectors:
- Azure
name: Virtual machine
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.compute/virtualmachines' group by 1,2;
status: active
tags:
  category:
  - Compute
