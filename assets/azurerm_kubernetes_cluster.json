
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "AKS cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.containerservice/managedclusters' group by 1,2;",
    "tags": {
        "category": ["Containers"]
    }
}
