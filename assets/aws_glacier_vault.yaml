connectors:
- AWS
name: S3 Glacier Vault
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::glacier::vault' group by 1,2;
status: active
tags:
  category:
  - Storage
