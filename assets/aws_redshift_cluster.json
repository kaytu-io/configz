
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "Redshift Cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::redshift::cluster' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
