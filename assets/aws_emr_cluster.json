
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "Elastic MapReduce (EMR) Cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::emr::cluster' group by 1,2;",
    "tags": {
        "category": ["Data Analytics"]
    }
}
