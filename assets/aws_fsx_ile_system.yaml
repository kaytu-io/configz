connectors:
- AWS
name: FSx File System
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::fsx::filesystem' group by 1,2;
status: active
tags:
  category:
  - Storage
