
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Azure Data Explorer cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.kusto/clusters' group by 1,2;",
    "tags": {
        "category": ["Data Analytics"]
    }
}
