
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Key vault",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.keyvault/vaults' group by 1,2;",
    "tags": {
        "category": ["Security"]
    }
}
