
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Virtual network subnet",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/virtualnetworks/subnets' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
