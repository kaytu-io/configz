connectors:
- AWS
name: Kinesis Data Streams
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::kinesis::stream' group by 1,2;
status: active
tags:
  category:
  - Data Analytics
