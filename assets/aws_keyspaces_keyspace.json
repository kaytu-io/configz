
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "Cassandra Keyspace",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::keyspaces::keyspace' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
