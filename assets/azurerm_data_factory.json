
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Azure Data Factory",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.datafactory/factories' group by 1,2;",
    "tags": {
        "category": ["Data Analytics"]
    }
}
