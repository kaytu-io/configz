
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "Workspace (VDI)",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::workspaces::workspace' group by 1,2;",
    "tags": {
        "category": ["End User"]
    }
}
