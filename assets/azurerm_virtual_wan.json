
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Virtual WAN",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/virtualwans' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
