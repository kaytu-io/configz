
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "Subnet",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ec2::subnet' group by 1,2;",
    "tags": {
        "category": ["Network"]
    }
}
