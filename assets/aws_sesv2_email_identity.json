
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "SES (V2) Email Identity",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::sesv2::emailidentity' group by 1,2;",
    "tags": {
        "category": ["End User"]
    }
}
