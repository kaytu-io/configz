
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Virtual desktop host pool",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.desktopvirtualization/hostpools' group by 1,2;",
    "tags": {
        "category": ["End User"]
    }
}
