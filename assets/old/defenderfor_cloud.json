
{
    "connectors": ["Azure"],
    "status": "invisible",
    "name": "Defender for Cloud",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('microsoft.security/pricings') group by 1,2;",
    "tags": {
        "category": ["Network"]
    }
}
