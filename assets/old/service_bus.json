
{
    "connectors": ["Azure"],
    "status": "invisible",
    "name": "Service Bus",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('microsoft.servicebus/namespaces') group by 1,2;",
    "tags": {
        "category": ["Application Integration"]
    }
}
