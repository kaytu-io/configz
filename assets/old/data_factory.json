
{
    "connectors": ["Azure"],
    "status": "invisible",
    "name": "Data Factory",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('microsoft.datafactory/factories') group by 1,2;",
    "tags": {
        "category": ["Data & Analytics"]
    }
}
