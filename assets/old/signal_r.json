
{
    "connectors": ["Azure"],
    "status": "invisible",
    "name": "SignalR",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('microsoft.signalrservice/signalr') group by 1,2;",
    "tags": {
        "category": ["Data & Analytics"]
    }
}
