
{
    "connectors": ["AWS"],
    "status": "invisible",
    "name": "Secrets Manager",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('aws::secretsmanager::secret') group by 1,2;",
    "tags": {
        "category": ["Security"]
    }
}
