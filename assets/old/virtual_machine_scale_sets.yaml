connectors:
- Azure
name: Virtual Machine Scale Sets
query: select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('microsoft.compute/virtualmachinescalesets') group by 1,2;
status: invisible
tags:
  category:
  - Compute
