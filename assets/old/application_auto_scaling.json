
{
    "connectors": ["AWS"],
    "status": "invisible",
    "name": "Application Auto Scaling",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('aws::applicationautoscaling::target') group by 1,2;",
    "tags": {
        "category": ["Compute"]
    }
}
