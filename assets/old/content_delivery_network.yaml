connectors:
- Azure
name: Content Delivery Network (CDN)
query: select connection_id, region, count(*) from kaytu_lookup where resource_type IN ('microsoft.cdn/profiles/endpoints') group by 1,2;
status: invisible
tags:
  category:
  - Network
