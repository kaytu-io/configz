
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Event Grid domain",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.eventgrid/domains' group by 1,2;",
    "tags": {
        "category": ["Data Analytics"]
    }
}
