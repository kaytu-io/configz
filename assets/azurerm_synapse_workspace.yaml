connectors:
- Azure
name: Azure Synapse Analytics Workspaces
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.synapse/workspaces' group by 1,2;
status: active
tags:
  category:
  - Data Analytics
