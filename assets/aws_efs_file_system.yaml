connectors:
- AWS
name: Elastic File System (EFS)
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::efs::filesystem' group by 1,2;
status: active
tags:
  category:
  - Storage
