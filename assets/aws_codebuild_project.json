
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "CodeBuild Project",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::codebuild::project' group by 1,2;",
    "tags": {
        "category": ["DevOps"]
    }
}
