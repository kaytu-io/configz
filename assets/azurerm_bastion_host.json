
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Azure Bastion",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/bastionhosts' group by 1,2;",
    "tags": {
        "category": ["Security"]
    }
}
