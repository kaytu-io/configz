
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "CloudFront (CDN) Distribution",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::cloudfront::distribution' group by 1,2;",
    "tags": {
        "category": ["Network"]
    }
}
