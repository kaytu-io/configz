
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "EBS Volume (Disk)",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ec2::volume' group by 1,2;",
    "tags": {
        "category": ["Storage"]
    }
}
