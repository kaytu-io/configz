
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "Direct Connect Gateway",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::directconnect::gateway' group by 1,2;",
    "tags": {
        "category": ["Network Gateways"]
    }
}
