connectors:
- Azure
name: Azure SQL Elastic Pool
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.sql/servers/elasticpools' group by 1,2;
status: active
tags:
  category:
  - Database
