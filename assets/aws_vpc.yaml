connectors:
- AWS
name: VPC (Virtual Network)
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ec2::vpc' group by 1,2;
status: active
tags:
  category:
  - Network
