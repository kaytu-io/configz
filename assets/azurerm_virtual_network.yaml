connectors:
- Azure
name: Virtual network
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/virtualnetworks' group by 1,2;
status: active
tags:
  category:
  - Network
