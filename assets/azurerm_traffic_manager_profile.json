
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Traffic Manager profile",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/trafficmanagerprofiles' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
