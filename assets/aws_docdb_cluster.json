
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "Amazon DocumentDB Elastic Cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::docdb::cluster' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
