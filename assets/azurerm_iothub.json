
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "IoT hub",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.devices/iothubs' group by 1,2;",
    "tags": {
        "category": ["IoT"]
    }
}
