
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "IAM Group",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::iam::group' group by 1,2;",
    "tags": {
        "category": ["Identity + Access"]
    }
}
