
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "VirtualHub Route server",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/virtualhubs' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
