
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Recovery Services vault",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.recoveryservices/vaults' group by 1,2;",
    "tags": {
        "category": ["Resiliency"]
    }
}
