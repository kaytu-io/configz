connectors:
- AWS
name: Local Gateway
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ec2::localgateway' group by 1,2;
status: active
tags:
  category:
  - Network Gateways
