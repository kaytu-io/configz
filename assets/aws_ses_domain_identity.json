
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "SES Email Identity (Legacy)",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ses::identity' group by 1,2;",
    "tags": {
        "category": ["End User"]
    }
}
