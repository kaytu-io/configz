
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Snapshot",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.compute/snapshots' group by 1,2;",
    "tags": {
        "category": ["Compute"]
    }
}
