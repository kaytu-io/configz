
{
    "connectors": ["AWS"],
    "status": "active",
    "name": "ElastiCache Cluster",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::elasticache::cluster' group by 1,2;",
    "tags": {
        "category": ["Database"]
    }
}
