
{
    "connectors": ["AWS"],
    "status": "inactive",
    "name": "VPN Gateway",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ec2::vpngateway' group by 1,2;",
    "tags": {
        "category": ["Network Gateways"]
    }
}
