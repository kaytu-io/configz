
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Container apps",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.app/containerapps' group by 1,2;",
    "tags": {
        "category": ["Containers"]
    }
}
