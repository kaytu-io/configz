
{
    "connectors": ["Azure"],
    "status": "active",
    "name": "Load balancer",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/loadbalancers' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
