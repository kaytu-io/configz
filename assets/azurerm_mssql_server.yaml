connectors:
- Azure
name: Microsoft SQL Azure Database Server
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.sql/servers' group by 1,2;
status: active
tags:
  category:
  - Database
