
{
    "connectors": ["Azure"],
    "status": "inactive",
    "name": "Private Link",
    "query": "select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/privatelinkservices' group by 1,2;",
    "tags": {
        "category": ["Networking"]
    }
}
